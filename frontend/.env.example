# Base path when SPA is served at /dashboard (e.g. from FastAPI).
# Run: VITE_BASE_PATH=/dashboard npm run build
# VITE_BASE_PATH=/dashboard

# API port for Vite dev proxy and Socket.IO (must match backend API_PORT). Default: 8000
# VITE_API_PORT=8001
